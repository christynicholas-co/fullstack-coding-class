create an array 5 -100
ITERATE through the array
print out numbers
print out specific message for numbers multiples of 7 "hello"
print out specific message for numbers multiples of 5 "jm"
else 5 and 7  "hello jm"


let arr = []
for (let i=0, 5 <= i >= 100, i++)
console.log (arr)


Problem 2:

Create a gallery of images. The gallery should be responsive. At desktop, there should be 3 images per row, at tablet 2 images, and at mobile 1 image.
Bonus feature: Each image will be 200 width by 200 height. If the original image size is not square, please use CSS to display square images on the gallery.
Bonus question: How would you go about taking data such as this and creating a photo gallery?

To make this code better :

!. create a js file to include code with a for loop to iterate over an array of images
    a. add the javascript source tag below the bootstrap tag in html
2. give the div in html an id to be targeted in the js file using document object model :
document.getElementById (imgArray)
3. Iterating through the array with a for loop will return all the images in the array
    a. display images to the web browser
I'm seriously stuggling with Javascript for loops and iterating through an array. this is my weakest area of all. i bombed this on every single test and quiz. I'm not even sure how to get better at this, practice doesn't help if I have to create a function for it and we haven't done it before. even still i have to research and navigate past projects and lessons to attempt it.(lots of time to figure out what will work forEach or forLoop) its all the simple things with these that i struggle with.


any advice or steps i can take to get better would be greatly appreciated.